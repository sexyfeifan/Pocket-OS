# 更新日志

## 1.9.13 — 2026-09-21

### 只读看板优化

**免密码访问**
- 如果没有设置查看密码，只读看板可以免密码访问
- 管理功能仍然需要密码

**全景时间轴单元格统一**
- 时间轴头部、日期单元格、时间轴行使用统一的 flex 布局
- 单元格最小宽度 60px，确保内容完整显示
- 事件标签最大宽度 180px，自动换行

**全屏模式精简**
- 全屏+全景时间轴模式下隐藏：header、nav、filters、timeline-tools、heading、footer
- 只保留时间轴表格内容

**自动刷新**
- 自动刷新间隔从 1 分钟改为 2 小时
- 页面隐藏时停止刷新，显示时恢复

### UI 重构

**项目详情页底部信息区重构**
- 准备事项、日期备注、TML 原文统一为 Accordion Row 组件
- 项目操作分组（项目完成并归档、复制为新项目）
- 项目管理重构为 2×2 设置栅格（项目状态、排期、推进方式、时区）
- 普通操作、生命周期操作、危险操作视觉分级
- 保存为项目模板默认折叠

**统一尺寸系统**
- 所有折叠行统一高度、左右 padding、标题字号、divider、Chevron 位置
- 展开内容与标题间距统一 16px
- 使用统一的 spacing tokens（8/16/24/32px）

## 1.9.12 — 2026-09-21

### UI 优化

**制作节点横向对齐**
- 将日期标签改为 inline-flex，与输入框在同一行
- 日期区域、操作按钮使用 align-items:center 确保水平对齐
- 标题"开始/结束"不参与主体控件对齐计算

**纵向间距系统统一**
- 所有折叠项使用统一的 padding:16px 0 0
- 统一分隔线位置，去除重复分隔线
- 展开内容与标题间距统一为 16px
- 底部时间信息添加明确的分组间距

**飞书导入按钮整合**
- 按钮已移动到"从拍摄通告导入"下方
- 样式与其他功能按钮完全一致
- 包含导入语义图标

## 1.9.12 — 2026-09-21

- 修复制作节点对齐问题：将 align-items 从 start 改为 center，确保操作按钮与日期输入框垂直居中对齐

## 1.9.11 — 2026-09-20

### 新建项目优化
- 移除内置模板（口播视频、商业拍摄、轻量短视频），只保留"标准工序"
- 保留用户自定义模板功能

### 飞书导入按钮整合
- 将"从飞书绑定 / 更新"按钮移动到"从拍摄通告导入"下方
- 添加飞书导入按钮图标，风格与其他图标一致

### 项目完成与归档
- 外放"项目完成并归档"按钮到项目编辑区域顶部
- 使用 primary 样式突出显示

### 节点完成按钮外放
- 将"确认实际完成"按钮外放到节点编辑区域的主操作栏
- 使用 primary 样式突出显示
- 保留"更多"菜单中的按钮作为备用

### 进度条优化
- 修改进度条计算逻辑，按实际日期更新
- 结束日已过算完成，结束日未过算进行中
- 不依赖是否人工确认完成

### UI/UX 优化
- 修复"更多"弹窗不消失问题，改用 popup 菜单
- 优化制作节点对齐，统一 node-heading、node-date-area、node-actions 的对齐
- 统一收起后行间距，确保所有 details 元素间距一致

### 测试覆盖
- 所有 46 个测试通过
- 语法检查通过

## 1.9.14 — 2026-09-20

- 草稿版本管理：GET /api/agent/v1/imports/:id 现在返回 isLatest 字段，标识是否是同一来源的最新 pending 草稿。
- 配额透明化：所有 Agent 响应头中添加 X-RateLimit-* 配额信息（IP 限制 240 次/分钟，Token 限制 90 次/分钟）。
- 429 错误优化：超限时返回 Retry-After 头和 retryAfter 字段，告知客户端需要等待多少秒。
- rate() 函数改进：返回 remaining 和 resetAt 信息，用于设置响应头。
- 更新版本号到 1.9.14。

## 1.9.13 — 2026-09-20

- 新增映射管理接口：GET /api/integrations/feishu/admin/mappings，获取所有已保存的映射列表。
- 新增映射详情接口：GET /api/integrations/feishu/admin/mappings/:key，获取单个映射详情。
- 新增从历史导入学习映射：POST /api/integrations/feishu/admin/mappings/learn，从已绑定项目中学习映射。
- Agent 映射查询优化：优先使用已保存的映射，如果没有则从历史导入中查询。
- 前端新增"从历史导入学习"按钮：在映射管理界面中，可一键从历史导入中学习映射。
- 映射操作按钮样式优化：添加 import-mapping-actions 样式类。
- 更新版本号到 1.9.13。

## 1.9.12 — 2026-09-20

- 修复 null 字段校验：fields 中的 null 值现在被接受并转换为空值，不再导致 400 错误。
- 改进错误信息：fetchedAt、updatedAt 等时间字段的错误信息现在包含格式示例（如 2026-09-20T22:00:00+08:00）。
- 新增 dry-run 校验接口：POST /api/agent/v1/imports/validate，提交前先校验数据格式，避免试错。
- fieldKeys 接受 null 或空字符串：使用默认值（title 默认 name，其他为空）。
- 更新 OpenAPI 文档：新增 /imports/validate 端点文档。
- 更新版本号到 1.9.12。

## 1.9.11 — 2026-09-13

- 新增批量忽略接口：Agent 可批量忽略选中的待确认草稿（POST /api/agent/v1/imports/batch-dismiss），单次最多操作 50 条。
- 新增映射写入接口：Agent 可保存飞书字段/节点映射（POST /api/agent/v1/mappings），后续导入相同模板的项目会自动应用。
- 前端新增批量选择 UI：在候选项目列表中支持全选、单选和批量忽略操作。
- 前端新增映射管理 UI：支持配置飞书字段 ID 与 Pocket OS 字段的对应关系。
- 更新 OpenAPI 规范：新增 /mappings（POST）和 /imports/batch-dismiss 端点文档。
- 更新版本号到 1.9.11。

## 1.9.10 — 2026-09-13

- 增加单向飞书项目导入：Agent 提交候选后，由用户手动选择绑定已有项目或创建项目，先预览再逐项确认；不按同名自动匹配，不自动同步，不修改飞书。
- 增加独立可撤销 Agent Token 与 OpenAPI 3.1 接口说明。未设置管理密码时禁用接入；Agent、管理者和只读查看者权限隔离，Agent 不能读取项目全库或操作排期、删除与备份。
- 项目资料支持项目号、横竖屏、广告项目（有广告 / 无广告 / 未填写）、合作渠道、大纲和脚本文档；合作渠道与发布平台分别记录。管理卡片和只读卡片支持业务资料展示与搜索。
- 支持大纲、脚本、分段拍摄、ACO、BCO、发布的真实计划日期。空日期不推算，不连续拍摄不合并；发布时间字段与发布节点不一致时由用户选择。
- 导入保留未勾选内容、备注、Canbox 绑定、人工完成、不适用和延期 / 暂停状态。空值清除与来源模板 / 字段映射变化需要额外确认。
- 增加稳定来源唯一绑定、请求去重、预览版本冲突保护、导入历史与提交回执恢复；解除绑定和撤回导入均先预览，撤回不删除新项目。
- 增加隔离数据回归测试，覆盖权限、无效日期、路径校验、重复请求、并发确认、分段日期、状态保留、重启恢复和只读字段隔离。

## 1.9.9 — 2026-09-07

- 新建项目先输入名称并选择模板，取消不创建空项目；新建、结构复制和 Canbox 新导入默认使用日期自动推进。旧项目不迁移，继续手动确认完成。
- 重排编辑页：名称与当前/下一节点摘要 → 已知日期与导入入口 → 项目日历 → 紧凑节点列表 → 准备事项/备注 → 项目管理。发布日期仅在发布节点编辑。
- 节点行保留日期和异常处理入口；低频的顺序、清空、删除、实际完成确认等收进“更多”。项目日历前置，支持首尾安排、今天和连续横向滚动。
- 增加按项目时区计算的自动状态：未开始、进行中、拍摄段间等待、按计划结束。结束日完整过去后才自动结束；不回写人工 done、不自动归档项目、不移动其他节点。
- 延期日期待定、暂停、等待反馈停止自动推进，并保留原计划为参考；具体日期调整有校验，过期计划恢复时要求新日期，只影响当前节点。
- 支持节点“不适用”，排除进度和日历但保留可恢复的原日期；空日期不推断已完成或延期。
- 自动结束与人工确认在进度和只读看板中明确区分。旧手动项目仍显示到期待核对提醒；模式/时区切换先预览再应用。
- 页面以服务器校时和项目时区显示状态，定时及回到前台刷新；仅查看不会写入状态或改动数据。补充跨日、时区、分段、旧项目兼容、异常处理和创建流程回归测试。

## 1.9.8 — 2026-09-06

- 工作台和只读看板统一按实际日期展示“当前节点何时结束 / 下一节点何时开始”，跳过没有日期的前置节点，支持并行节点与超过七天的后续安排。
- 不再把空日期判断为排期未完成；移除“部分已排 / 部分已确认”，只手动标记的项目进入“待反馈 / 调整中”。完成进度仍按节点勾选统计，不因没填日期推断完成状态。
- 多段拍摄尚有当前或后续段时，已经过去的拍摄段不单独报逾期；整项节点超过最后结束日才提醒核对。
- 只读项目详情顶部增加独立横向排期日历，下方保留完整项目信息与节点列表。
- 全景时间轴默认两周细览，可鼠标横拖、触控板横滑和键盘连续浏览日期；支持首个 / 最后安排、今天、指定日期跳转。
- 短节点名称与日期完整换行，标签独立分层，不通过放大色条篡改实际日期长度。两周 / 六周切换和自动刷新保留当前浏览位置。
- 只读横拖仅平移视野，无项目写入；新增概览语义、分段与时间轴几何回归测试。

## 1.9.7 — 2026-09-06

- 新增 `/view` 正式只读入口（兼容 `/canbox`），含工作概览、项目卡片、周/月日历、全景时间轴、只读详情、搜索筛选和全屏。
- 新增独立查看密码与服务端权限隔离，查看数据采用字段白名单；查看者不能修改项目、导出备份或读取管理配置。
- 工作台与看板共用日期、分段和进度统计，未排期节点纳入总进度，自定义节点不会遗漏。
- 新增今日/七天/逾期/待处理概览，项目状态与排期确认状态独立；确认后改期会撤回确认，但不会移动其他节点。
- 新增口播、商业拍摄、短视频模板、自定义模板及结构复制，不复制日期、完成状态、通告绑定或备注。
- 新增紧凑排期编辑，返回保留原视图、筛选和位置，进入项目从顶部开始。
- 服务端记录最近 30 次已保存排期变更，刷新后保留；支持逐节点预览恢复，保留已有节点的名称和完成状态。
- Canbox 日期应用/绑定与 TML 支持前后日期对照、勾选应用；过期预览不能覆盖已发生变化的排期。
- 看板实时更新、断线提示、回到前台重连；手机周历使用逐日列表。
- 修正通告单导出及公开日历 API 的多段拍摄间隔显示；新增权限和工作流回归测试、GitHub CI。
- Docker 更新到 Node.js 22，每种架构构建时运行完整回归；忽略本地 `.env` 与用户数据。

## 1.9.6 — 2026-09-06

- 完全移除倒排、通告顺推和拖拽/日历放置时的相邻工序推挤；所有节点独立安排，可重叠进行。
- 项目打开即可编辑，移除重复的只读/编辑区域和离开编辑确认；新建自动聚焦名称。
- 未安排节点直接选择日期；新增单节点前移/后移一天、节点改名、顺序按钮、手动增加拍摄段和最近 30 次排期撤销。
- 时间轴色块点击直接进入节点编辑；月历日期支持安排节点和备注；修复多段拖拽锚点、自定义工序拖拽和跨项目误放。
- 刷新不再恢复已删除工序、重置顺序或用旧通告日期覆盖人工排期。
- TML 只更新列出的节点，保留其他节点；增加年份选择和完整日期预览，拒绝非法范围。
- 工序可独立标记完成，修改项目名称等普通字段不会自动撤回项目完成。
- 自动保存改为顺序队列，增加保存状态、未保存草稿恢复和离开提醒，避免连续编辑中断请求引发错误冲突。
- 增加覆盖真实前端操作入口的排期回归测试；修复手机日期框布局与返回入口遮挡。
- 锁定传递依赖 qs 6.16.0，修复本轮构建发现的解析器漏洞。

## 1.9.5 — 2026-08-31

- 修复 Canbox 导入刷新后丢失、首次连接 403、绑定空选题不生成排期的问题。
- 修复 Canbox 发布日期与发布工序相差一天的问题，并迁移旧版不一致数据。
- 支持不连续拍摄段分别显示和编辑。
- 备份恢复改为完整、原子替换，拒绝非法或重复选题 ID。
- 多设备版本冲突不再自动覆盖，改为由用户选择服务器版或当前版。
- 统一发布日期与发布工序的同步规则，修复手动日期和工序时长计算。
- 修复工序设置拖拽异常；工序排序不再自动改变日期。
- TML 导出保留工序日期内备注，并支持多段拍摄与日期开头的备注重新解析。
- 操作日志改为最新优先、支持分页，并补充主要修改动作。
- 增加可选整站密码 `POCKET_OS_PASSWORD` 和后端回归测试。
