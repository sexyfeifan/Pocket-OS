services:
  pocket-os:
    image: sexyfeifan/pocket-os:latest
    build: .
    ports:
      - "8080:8080"
    volumes:
      - pocket-data:/app/data
    restart: unless-stopped

volumes:
  pocket-data:
