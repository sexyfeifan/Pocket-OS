services:
  pocket-os:
    image: sexyfeifan/pocket-os:latest
    platform: linux/amd64
    ports:
      - "8080:8080"
    volumes:
      - pocket-data:/app/data
    restart: unless-stopped

volumes:
  pocket-data:
